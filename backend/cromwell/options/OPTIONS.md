This folder is meant for temporarily storing workflow options for Cromwell.
At least the refresh_token is needed, the file format will initially be:

```
{
  "refresh_token": "foo"
}
```

These files should never be committed.
